# Jarvis UI test

Design sandbox on the Desktop. **Do not edit the live UI in `Desktop/JARVIS` until a design is chosen.**

This folder now holds a **copy of the current Jarvis desktop UI** with:

- the ice orb from Jarvis-chat
- Outfit + Orbitron + JetBrains fonts from the new design

## Run

```bash
cd "C:\Users\saivenkat\Desktop\jarvis UI test"
npm run dev
```

Open **http://127.0.0.1:3001**

The live app in `Desktop/JARVIS` is not modified.

## Later: move into live Jarvis

See [INTEGRATE.md](./INTEGRATE.md).
